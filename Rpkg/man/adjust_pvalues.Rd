% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pvalueAdjustment.R
\name{adjust_pvalues}
\alias{adjust_pvalues}
\title{Apply p-value adjustment with optional independent filtering}
\usage{
adjust_pvalues(
  res,
  independentFiltering = FALSE,
  alpha = 0.05,
  method = "nested_BH"
)
}
\arguments{
\item{res}{Data frame with columns \code{gene}, \code{pvalue}, and (when
\code{independentFiltering = TRUE}) \code{baseMean}.}

\item{independentFiltering}{Logical. Apply independent filtering before
adjustment.}

\item{alpha}{Numeric. FDR level (default 0.05).}

\item{method}{Character. Adjustment method: \code{"nested_BH"} (default) or
any method accepted by \code{p.adjust} (e.g. \code{"BH"}).}
}
\value{
\code{res} with \code{padj} added (and \code{padj_gene} for
\code{"nested_BH"}).
}
\description{
Dispatches on \code{method}:
\itemize{
\item \code{"nested_BH"}: optionally masks low-count events via independent
filtering, then runs the two-stage nested BH procedure (\code{nested_bh}).
Adds \code{padj_gene} (gene-level) and \code{padj} (event-level) columns.
\item any \code{p.adjust} method (e.g. \code{"BH"}, \code{"bonferroni"}):
delegates directly to \code{pvalueAdjustment}, which handles independent
filtering internally and adds a single \code{padj} column.
}
}
